---
title: "Sponsaurus Blog"
blog: blog
---

<!-- This example requires Tailwind CSS v2.0+ -->
<div
  class="relative bg-gray-50 pt-16 pb-20 px-4 sm:px-6 lg:pt-24 lg:pb-28 lg:px-8"
>
  <div class="absolute inset-0">
    <div class="bg-white h-1/3 sm:h-2/3"></div>
  </div>
  <div class="relative max-w-7xl mx-auto">
    <div class="sm:w-4/5 mx-auto text-center pb-4 border-b-2 border-gray-300">
      <h2
        class="text-3xl tracking-tight font-extrabold text-gray-900 sm:text-4xl"
      >
        From the blog 🦕
      </h2>
      <p class="mt-3 max-w-2xl mx-auto text-lg text-gray-500 sm:mt-4">
        Mostly product updates and devlogs. Occasional articles on building,
        managing and growing your email newsletter.
      </p>
    </div>

    <div class="mt-12 max-w-lg mx-auto grid gap-5 lg:grid-cols-3 lg:max-w-none">
      <% blog.articles.each do |article| %>
      <div
        class="
          transform
          hover:scale-105
          ease-in-out
          duration-150
          flex flex-col
          rounded-lg
          shadow-lg
          overflow-hidden
        "
      >
        <div class="flex-shrink-0">
          <img
            class="h-48 w-full object-cover"
            src="<%= article.data.thumbnail %>"
            alt=""
          />
        </div>
        <div class="flex-1 bg-white p-6 flex flex-col justify-between">
          <div class="flex-1">
            <a href="<%= article.url %>" class="block mt-2">
              <p class="text-xl font-semibold text-gray-900">
                <%= article.title %>
              </p>
              <p class="mt-3 text-base text-gray-500"><%= article.summary %></p>
              <div class="mt-3">
                <a
                  href="<%= article.url %>"
                  class="
                    text-base
                    font-semibold
                    text-indigo-600
                    hover:text-indigo-500
                  "
                >
                  Keep reading →
                </a>
              </div>
            </a>
          </div>
          <%= blog_card_bottom_helper(article) %>
        </div>
      </div>
      <% end %>
    </div>
  </div>
</div>
